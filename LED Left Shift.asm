	ORG 00H

MAIN:	MOV P1,#0FFH
	JNB P2.1,NYALA
	JMP MAIN

NYALA:	CLR P1.7
	ACALL DELAY
	SETB P1.7
	JB P2.1,MAIN

	CLR P1.6
	ACALL DELAY
	SETB P1.6
	JB P2.1,MAIN

	CLR P1.5
	ACALL DELAY
	SETB P1.5
	JB P2.1,MAIN

	CLR P1.4
	ACALL DELAY
	SETB P1.4
	JB P2.1,MAIN

	CLR P1.3
	ACALL DELAY
	SETB P1.3
	JB P2.1,MAIN

	CLR P1.2
	ACALL DELAY
	SETB P1.2
	JB P2.1,MAIN

	CLR P1.1
	ACALL DELAY
	SETB P1.1
	JB P2.1,MAIN

	CLR P1.0
	ACALL DELAY
	SETB P1.0
	JB P2.1,MAIN

	JMP NYALA

DELAY:	MOV R7,#2
ULANG:	DEC R7
	CJNE R7,#0,ULANG
	RET

END
